import { FormSchemeProps } from '@jimu/components';

export enum KEY_MAP {
  /* SEARCH_KEY_MAP_CREATE */
}

export const {
  /* SEARCH_KEY_MAP_EXPORT */
} = KEY_MAP;

const model: FormSchemeProps['model'] = {
  /* SEARCH_MODEL */
};

export default model;
